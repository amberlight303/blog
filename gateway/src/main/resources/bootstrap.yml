spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:9091
      fail-fast: true
      username: configUser
      password: configPassword



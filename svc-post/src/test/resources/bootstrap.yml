spring:
  application:
    name: post-service
  cloud:
    config:
      uri: http://localhost:9081
      fail-fast: true
      username: configUser
      password: ${CONFIG_SERVICE_PASSWORD}
      #      password: configPassword
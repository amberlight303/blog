spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: auth
  cloud:
    config:
      uri: http://localhost:9081
      fail-fast: true
      username: configUser
      password: configPassword

